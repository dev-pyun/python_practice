Syntactic sugar :
자주 쓰이는 표현을 더 간략하게 쓸 수 있게 해주는 문법

예시

x = x + 1 을 더 간단하게!
x += 1

x = x * 2
x *= 2

x = x - 3
x -= 3

x = x / 2
x /= 2

x = x % 2
x %= 2

즉,
변수 = 변수 (연산자) 숫자(or another 변수)
변수 (연산자)= 숫자(or another 변수)

이 두줄이 같은 의미이다.